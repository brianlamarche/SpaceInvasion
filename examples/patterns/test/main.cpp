#include <iostream>
#include <string>
#include "GameStateMenu.h"
#include <vector>

using namespace std;


enum TEST {a, b, c};

class Test
{
	TEST Find()
		{
			return a;
		}

	
};

int main()
{
	GameStateMenu * menu = new GameStateMenu();

	menu->Test(5);

	return 0;
}
